<h1>Listing All Products</h1>
<table>
	<thead>
    <tr>
    <th>Merchant ID</th>
      <th>Title</th>
      <th>Description</th>
      <th>Price</th>
      <th>Buy</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @product.each do |product| %>
      <tr>
        <td><%= product.merchant_id %></td>
        <td><%= product.title %></td>
        <td><%= product.description %></td>
        <td><%= product.price %></td>
        <td><%= link_to 'Create Purchase' %>BUY</a></td>  <!--user_purchase(:user_id => ) -->
       
      </tr>
    <% end %>
    
  </tbody>

</table>

<p><%= will_paginate @product %></p> 